import { Router } from "express";

const blogRoutes = () => {
  const blogRouter = Router();

  // GET requests
  // POST requests
  // PUT requests
  // DELETE requests
  return blogRouter;
};

export default blogRoutes;
